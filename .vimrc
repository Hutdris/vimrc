
" 行游標提示
set cursorline
" 滑鼠控制
set mouse+=a

" 加入utf8編碼
set enc=utf8


" 設定自動縮排 為了防止剪下貼上造成排版錯誤 可以先用
" :set paste 後再貼上
set ai

" 設定行號
" set nu

" 設定相對行號
set relativenumber

" 空白取代tab
set expandtab

" 定義縮排
set tabstop=4
set shiftwidth=4

" 自動換行
set wrap
" set nowrap 

" 和系統剪貼簿共用
set clipboard=unnamed

set wildmenu " visual autocomplete for command menu

